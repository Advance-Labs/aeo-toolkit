/**
 * Re-export shim — contact extraction now lives in `@aeo/backlinks`.
 */
export { extractContacts, extractEmails, extractSocials } from '@aeo/backlinks';
export type { ContactInfo, SocialHandle, SocialNetwork } from '@aeo/backlinks';
