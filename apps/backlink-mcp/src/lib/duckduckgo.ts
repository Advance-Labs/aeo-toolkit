/**
 * Re-export shim — the DuckDuckGo adapter now lives in `@aeo/backlinks`.
 */
export { search, parseResults, unwrapRedirect, buildSearchUrl } from '@aeo/backlinks';
export type { SearchResult, SearchOutcome } from '@aeo/backlinks';
