{
  "name": "@aeo/blogging-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonomous multi-agent blogging pipeline: research GSC query gaps, draft with Groq, edit, dedup via Jaccard fingerprints, schedule, publish, and self-correct underperformers. Built on @aeo/google-api and @aeo/llm (BYOK). Runs on a GitHub Actions schedule or manually.",
  "license": "MIT",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/run.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@aeo/google-api": "workspace:*",
    "@aeo/llm": "workspace:*",
    "@aeo/storage": "workspace:*",
    "@aeo/types": "workspace:*"
  },
  "devDependencies": {
    "@aeo/config": "workspace:*",
    "@types/node": "^22.10.2",
    "rimraf": "^6.0.1",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
